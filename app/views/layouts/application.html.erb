<!DOCTYPE html>
<html>
<head>
  <title>youShare</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag 'application', 'data-turbo-track': 'reload' %>
  <%= stylesheet_link_tag 'navbar', 'data-turbo-track': 'reload' %> <!-- Añadimos la referencia al nuevo archivo CSS -->
  <%= stylesheet_link_tag 'userPicture', 'data-turbo-track': 'reload' %>
  
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>

</head>

<body>
  <!-- Barra de navegación -->
  <%= render 'navbar/navbar' %>
  
  <!-- Mensajes de notificación aquí -->
  <div id="messages">
    <% flash.each do |name, msg| %>
      <% if msg.is_a?(String) %>
        <div class="flash <%= name %>"><%= msg %></div>
      <% end %>
    <% end %>
  </div>

  <!-- Contenido principal -->
  <%= yield %>

  <!-- Aquí podrías añadir un pie de página u otros elementos que se muestren en todas las páginas -->
</body>
</html>
